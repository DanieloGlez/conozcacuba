package dto.nom;

import dto.Dto;

public class CompanyServiceDto extends NomenclatorDto implements Dto {
    // Constructors
    public CompanyServiceDto(int id, String name) {
        super(id, name);
    }
}
